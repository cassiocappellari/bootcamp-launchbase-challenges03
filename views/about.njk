{%extends 'layout.njk'%}

{%block head%}
    <title>Rocketseat</title>
{%endblock%}

{%block content%} 

<div class="middle-content">
    <div class="content">
        <h2>{{data.title}}</h2>
        <p>{{data.about}}</p>
        <a href="">{{data.buttonText}}</a>
    </div>
    <div class="background">
        <img src="{{data.rocketseatImg}}" alt="Melhores Tecnologias">
    </div>
</div>

{%endblock%}